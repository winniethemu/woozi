export enum MessageType {
  SET_USER = 'SET_USER',
}

export enum GameStatus {
  PENDING = 'PENDING',
  PLAYING = 'PLAYING',
  COMPLETED = 'COMPLETED',
}

export enum StoneType {
  BLACK = 'black',
  WHITE = 'white',
}
